# reference clones (upstream python-haiku, old dario haiku.c, q, postgpt) — local only
reffs/
# build artifacts
subjectivity
*.o
*.dSYM
# per-life memory (the scar persists locally, not in source)
*.mem
*.mem.tmp
